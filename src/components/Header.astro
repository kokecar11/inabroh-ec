---
interface Props {
	title: string
}

const { title } = Astro.props
---

<div class="container mx-auto px-6">
	<div class="mb-2 flex items-center">
		<h2 class="flex-1 text-3xl font-semibold">{title}</h2>
		<slot />
	</div>
	<hr class="border border-primary/10" />
</div>
